<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>de.wwu.scdh.oxygen.alea</groupId>
    <artifactId>scdh-alea-oxygen-extension</artifactId>
    <version>1.0.3</version>
    
    <name>scdh-alea-oxygen-extension</name>
    <url>https://zivgitlab.uni-muenster.de/SCDH/hees-alea/oxygen-framework</url>

    <properties>
	<oxygen.sdk.version>22.1.0.0</oxygen.sdk.version>
	<distribution.url>https://zivgitlab.uni-muenster.de/api/v4/projects/${env.CI_PROJECT_ID}/packages/maven</distribution.url>
	<!--distribution.url>https://zivgitlab.uni-muenster.de/api/v4/projects/2415/packages/maven</distribution.url-->
    </properties>

    <repositories>
	<repository>
	    <id>oxygenxml</id>
	    <name>oXygen XML SDK Maven Repository</name>
	    <url>https://oxygenxml.com/maven/</url>
	</repository>
	<repository>
	    <id>gitlab-maven</id>
	    <url>${distribution.url}</url>
	</repository>
    </repositories>

    <distributionManagement>
	<repository>
	    <id>gitlab-maven</id>
	    <url>${distribution.url}</url>
	</repository>
	<snapshotRepository>
	    <id>gitlab-maven</id>
	    <url>${distribution.url}</url>
	</snapshotRepository>
    </distributionManagement>

    
    <dependencies>
	<dependency>
            <groupId>com.oxygenxml</groupId>
            <artifactId>frameworks</artifactId>
            <version>${oxygen.sdk.version}</version>
            <type>zip</type>
            <scope>provided</scope>
	</dependency>
    </dependencies>

    <build>
	<plugins>
	    
	    <plugin>
		<groupId>io.xspec.maven</groupId>
		<artifactId>xspec-maven-plugin</artifactId>
		<configuration>
                    <!--coverage>true</coverage-->
		    <generateSurefireReport>true</generateSurefireReport>
		    <testDir>test/xspec</testDir>
		</configuration>
		<executions>
                    <execution>
			<phase>test</phase>
			<goals>
                            <goal>run-xspec</goal>
			</goals>
                    </execution>
		</executions>
		<dependencies>
		    <dependency>
			<groupId>net.sf.saxon</groupId>
			<artifactId>Saxon-HE</artifactId>
			<version>9.9.1-6</version>
		    </dependency>
		</dependencies>
            </plugin>

            <plugin>
		<artifactId>maven-antrun-plugin</artifactId>
		<groupId>org.apache.maven.plugins</groupId>
		<version>1.7</version>
		<configuration>
                    <target>
			<zip destfile="${project.build.directory}/classes/frameworks.zip" duplicate="fail">
			    <zipfileset dir="frameworks"></zipfileset>
			    <!--zipfileset dir="default-oxygen-frameworks">
				<include name="tei/**" />
			    </zipfileset-->
			    <zipfileset dir=".">
				<include name="README.md"/>
			    </zipfileset>
			</zip>
                    </target>
		</configuration>
		<executions>
                    <execution>
			<phase>prepare-package</phase>
			<goals>
			    <goal>run</goal>
			</goals>
                    </execution>
		</executions>
            </plugin>

            <plugin>
		<groupId>org.apache.maven.plugins</groupId>
		<artifactId>maven-dependency-plugin</artifactId>
		<version>2.8</version>
		<executions>
                    <!-- Copy the default oXygen frameworks. -->
                    <execution>
			<id>copy-frameworks</id>
			<phase>generate-resources</phase>
			<goals>
			    <goal>unpack</goal>
			</goals>
			<configuration>
			    <artifactItems>
				<artifactItem>
				    <groupId>com.oxygenxml</groupId>
				    <artifactId>frameworks</artifactId>
				    <type>zip</type>
				</artifactItem>
			    </artifactItems>
			    <outputDirectory>${basedir}/default-oxygen-frameworks</outputDirectory>
			    <overWriteReleases>false</overWriteReleases>
			    <overWriteSnapshots>true</overWriteSnapshots>
			    <stripVersion>true</stripVersion>
			</configuration>
		    </execution>
		</executions>
            </plugin>

	    <plugin>
              <artifactId>maven-clean-plugin</artifactId>
              <version>2.5</version>
              <configuration>
                <filesets>
                  <fileset>
                    <directory>${basedir}/default-oxygen-frameworks</directory>
                    <!--excludes>
                      <exclude>README.txt</exclude>
                      <exclude>.svn/**</exclude>
                    </excludes-->
                  </fileset>
                </filesets>
              </configuration>
            </plugin>

	    <plugin>
		<groupId>org.codehaus.mojo</groupId>
		<artifactId>xml-maven-plugin</artifactId>
		<executions>
		    <execution>
			<goals>
			    <goal>transform</goal>
			</goals>
		    </execution>
		</executions>
		<configuration>
		    <transformationSets>
			<transformationSet>
			    <dir>.</dir>
			    <includes>
				<include>pom.xml</include>
			    </includes>
			    <stylesheet>pages/descriptor.xsl</stylesheet>
			</transformationSet>
		    </transformationSets>
		</configuration>
		<dependencies>
		    <dependency>
			<groupId>net.sf.saxon</groupId>
			<artifactId>Saxon-HE</artifactId>
			<version>10.2</version>
		    </dependency>
		</dependencies>
	    </plugin>
	    
	</plugins>
    </build>

</project>
